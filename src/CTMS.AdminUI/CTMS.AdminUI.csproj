<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    TargetFramework / Nullable / ImplicitUsings / TreatWarningsAsErrors are inherited
    from the repo-root Directory.Build.props. Only UI-specific settings live here.
  -->
  <PropertyGroup>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
  </PropertyGroup>

</Project>
