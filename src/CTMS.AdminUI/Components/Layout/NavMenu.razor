@implements IDisposable
@inject NavigationManager Nav
@inject CtmsApiClient Api

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">CTMS Admin</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="applications" Match="NavLinkMatch.Prefix">
                <span class="bi bi-folder2-open-nav-menu" aria-hidden="true"></span> Applications
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="languages" Match="NavLinkMatch.Prefix">
                <span class="bi bi-globe2-nav-menu" aria-hidden="true"></span> Languages
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="dashboard" Match="NavLinkMatch.Prefix">
                <span class="bi bi-info-circle-nav-menu" aria-hidden="true"></span> Dashboard
            </NavLink>
        </div>

        @if (_appCode is { } code)
        {
            <div class="nav-context px-3 pt-3 pb-1 text-uppercase small">
                @(_appName ?? code)
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}")" Match="NavLinkMatch.All">
                    <span class="bi bi-info-circle-nav-menu" aria-hidden="true"></span> Overview
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/grid")">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Grid
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/import")">
                    <span class="bi bi-box-nav-menu" aria-hidden="true"></span> Import
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/review")">
                    <span class="bi bi-check2-square-nav-menu" aria-hidden="true"></span> Review
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/missing")">
                    <span class="bi bi-key-nav-menu" aria-hidden="true"></span> Missing
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/publish")">
                    <span class="bi bi-box-nav-menu" aria-hidden="true"></span> Publish
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="@($"applications/{code}/history")">
                    <span class="bi bi-clock-history-nav-menu" aria-hidden="true"></span> History
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private string? _appCode;
    private string? _appName;

    protected override void OnInitialized()
    {
        Nav.LocationChanged += OnLocationChanged;
        _ = SyncToLocationAsync();
    }

    private void OnLocationChanged(object? sender, Microsoft.AspNetCore.Components.Routing.LocationChangedEventArgs e)
        => _ = SyncToLocationAsync();

    private async Task SyncToLocationAsync()
    {
        var previous = _appCode;
        _appCode = ExtractAppCode(Nav.ToBaseRelativePath(Nav.Uri));

        if (!string.Equals(_appCode, previous, StringComparison.Ordinal))
        {
            _appName = null;
            await InvokeAsync(StateHasChanged);

            if (_appCode is { } code)
            {
                var result = await Api.GetApplicationAsync(code);
                if (result.IsSuccess && string.Equals(_appCode, code, StringComparison.Ordinal))
                {
                    _appName = result.Value!.Name;
                    await InvokeAsync(StateHasChanged);
                }
            }
        }
    }

    private static string? ExtractAppCode(string relativePath)
    {
        var path = relativePath.Split('?', '#')[0];
        var segments = path.Split('/', StringSplitOptions.RemoveEmptyEntries);
        return segments.Length >= 2 && segments[0].Equals("applications", StringComparison.OrdinalIgnoreCase)
            ? Uri.UnescapeDataString(segments[1])
            : null;
    }

    public void Dispose() => Nav.LocationChanged -= OnLocationChanged;
}
