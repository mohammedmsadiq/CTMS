@page "/projects/{ProjectId:guid}/bundles"
@attribute [Authorize(Policy = AuthorizationPolicies.CanRead)]

<PageTitle>Bundles — CTMS Admin</PageTitle>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="projects">Projects</a></li>
        <li class="breadcrumb-item"><a href="@($"projects/{ProjectId}")">Project</a></li>
        <li class="breadcrumb-item active" aria-current="page">Bundles</li>
    </ol>
</nav>

<h1 class="h3">Published bundles</h1>

@* TODO: WS4 — wire this up once backend-core ships GET /api/projects/{projectId}/bundles/{locale}
   and the publish action. See docs/api.md "Planned endpoints". *@
<div class="alert alert-info">
    Not available yet. The published-bundle delivery and publish endpoints are planned for
    WS4 and do not exist on the current API.
</div>

@code {
    [Parameter] public Guid ProjectId { get; set; }
}
